// Ejercicio Naves

import kotlin.concurrent.thread
import kotlin.random.Random

fun main(args: Array<String>) {

    var tet_prueba = Tet()
    var dron_prueba = Dron()

    tet_prueba.crearTet(200,"Dron") // se crea el almacén del Tet

    for (i in 0 until tet_prueba.almacen.size) {

    }

}